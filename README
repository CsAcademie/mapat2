### Developpement

- npm install
- npm start

### Déploiement

- npm install
- Linux: Directement via CI
- Windows:
- * Vérifier que privateKey.pfx est présent
- * `$Env:GITHUB_TOKEN="xxx"` puis `npm run publish-win`
- * ou `GH_TOKEN=xxx npm run publish-win`
